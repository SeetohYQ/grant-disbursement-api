const apiUrl = `http://localhost:3000`;

const sqlConfig = {
    host     : 'localhost',
    user     : 'new_user',
    password : 'password',
    database : 'my_db'
};

module.exports = { apiUrl, sqlConfig }